services:
  app:
    build:
      context: ..
      dockerfile: backend/Dockerfile.dev
    container_name: backend
    ports:
      - "3000:3000"
    volumes:
      - .:/app/backend
      - nodeModules:/app/backend/node_modules
    env_file:
      - .env


volumes:
  nodeModules: