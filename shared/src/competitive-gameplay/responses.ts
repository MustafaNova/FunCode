export interface WrongRes {
    playerName: string;
}

export interface WinRes {
    playerName: string;
    solution: string;
}

export interface LoseRes {
    playerName: string;
    solution: string;
}
